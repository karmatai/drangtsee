import React from 'react';
import { Box, Typography } from '@mui/material';

function Profile() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Profile
      </Typography>
      {/* Add functionality to display and edit user's profile */}
    </Box>
  );
}

export default Profile;